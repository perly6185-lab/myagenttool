{
  "name": "@myagenttool/session-engine",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Playwright browser pipeline for session-backed site plugins: persistent-profile context launch (reusing logged-in sessions) and lazy-hydration scrolling. Zero dependencies of its own — the hoisted root playwright devDependency provides the browser.",
  "type": "module",
  "exports": {
    "./launch": "./src/launch.mjs",
    "./scroll": "./src/scroll.mjs"
  },
  "scripts": {
    "test": "node --test ./test/*.test.mjs"
  }
}
