# “我的私教”分阶段开发计划

状态：M5 已完成，M6 评测可信度建设中

版本：Draft 0.3

日期：2026-08-26

实施分支：`feat/private-tutor-m6-evaluation`

## 1. 方向调整

“我的私教”现在定义为当前账号本人的长期 AI 学习空间。

不再把家长、孩子、监护、多孩子切换和儿童模式作为产品基础。现有实现中的 `learnerId` 暂时保留为稳定的数据归属键，以避免破坏已经完成的学习证据；新代码和产品语义逐步迁移为一个账号一份 `LearningProfile`。

首个数学纵向闭环继续作为内容包验证，但平台必须能扩展到大学教材、专业课程、职业技能和用户资料。

## 2. 当前实现快照

| 能力 | 状态 | 迁移结论 |
| --- | --- | --- |
| 自适应摸底与判题 | 核心闭环完成 | 保留，解除年级和儿童文案绑定 |
| 知识地图与学习计划 | 核心闭环完成 | 保留，计划时长改为用户可配置 |
| 每日教学状态机 | 核心闭环完成 | 保留，改称学习会话，不固定儿童 20 分钟模式 |
| 实时语音 | 工程闭环完成 | 保留，扩展专业术语和多学科表达 |
| 动态视觉 | 数学演示完成 | 作为数学内容插件，不要求所有学科使用同一模板 |
| 错题与间隔复习 | 核心闭环完成 | 保留，归属当前账号唯一学习档案 |
| SQLite、幂等、导出与删除 | 工程闭环完成 | 保留，界面和合同改为“我的学习数据” |
| 多孩子、监护邀请、儿童模式 | 已实现但不再属于目标方向 | 从主流程移除，制定兼容迁移后再删除旧合同 |
| 家庭周报和儿童试点治理 | 已实现但不再属于目标方向 | 停止扩展；通用学习质量门禁继续保留 |
| 内容审核与版本发布 | 工程闭环完成 | 保留为平台后台，不作为“老师”角色展示给普通用户 |
| 通用学习内容包 | 五类内置内容包与学科插件基线完成 | 继续做真实课程内容、模型校准和生产级沙箱加固 |
| “我的设置”中的 AI 私教配置 | 界面基线已调整 | 下一步持久化并接入教学编排 |

## 3. 产品主路径

    登录当前账号
    → 读取或创建唯一学习档案
    → 在“我的设置”选择学习内容和目标
    → 摸底或从指定章节开始
    → 知识地图与计划
    → AI 讲解、练习和复习
    → 用独立新题与延迟复测更新掌握度

主路径不得再要求：

- 添加或选择孩子；
- 设置家长 PIN；
- 进入或退出儿童模式；
- 在家庭与专业角色空间之间切换；
- 接受监护邀请或查看家庭周报。

## 4. 架构原则

1. 一个账号一份默认学习档案。
2. 账号边界仍由服务端强制执行，不能因为移除多孩子功能而放松跨账号隔离。
3. 内容统一抽象为版本化 `LearningContentPackage`。
4. 教材、大学课程和专业技能使用同一知识图谱合同，不强制依赖年级。
5. AI 私教是教学行为配置，不是独立人员或后台角色。
6. AI 负责组织讲解，确定性引擎和证据规则负责判题与掌握判断。
7. 历史内容、作答、策略和学习模型保持版本化与可追溯。
8. 旧家庭合同先兼容、后迁移、最后删除，避免学习记录丢失。

## 5. 新阶段计划

### M0：方向与入口收敛

状态：进行中。

交付：

- 更新产品需求和开发计划；
- 主入口改为当前账号的唯一学习档案；
- 移除家长、孩子选择、PIN 和儿童模式主路径；
- “我的设置”改为学习偏好、学习内容、AI 私教和学习数据；
- 保持既有数学学习闭环可运行；
- 记录旧接口兼容边界。

退出标准：

- 普通用户不再看到家庭或多孩子流程；
- 新用户一次建立自己的学习档案后直接进入学习；
- 现有用户学习证据不丢失；
- 前端测试、类型检查和生产构建通过。

### M1：单用户领域模型迁移

目标：把内部领域语义从家庭儿童模型迁移到个人学习模型。

交付：

- `LearningProfile` 与当前账号建立唯一约束；
- 新增 `/api/private-tutor/profile` 当前用户接口；
- 将业务合同中的 `learnerId` 逐步改为 `learningProfileId`；
- 将 guardian 权限链接改为账号所有权；
- 删除新建多个学习档案的入口和接口能力；
- 提供旧数据迁移脚本与回滚检查；
- 保留跨账号访问拒绝测试。

退出标准：

- 同一账号无法创建第二个默认档案；
- 登录后无需 child-mode 会话即可学习；
- 旧学习记录能映射到唯一档案；
- 所有读写都按账号和档案双重校验。

### M2：通用学习内容包

目标：解除系统对七年级方程演示内容的结构绑定。

交付：

- `LearningContentPackage`、`Module`、`Topic`、`KnowledgeComponent`；
- 教材、大学课程、职业技能和用户资料四种来源；
- 内容版本、来源、许可和适用阶段；
- 学习目标和先修关系；
- 学科插件：确定性判题、表达规范化和可视化能力声明；
- 将现有数学内容迁移为第一个 `math-foundations` 内容包。

退出标准：

- 新增内容包不需要修改学习会话主状态机；
- 大学课程可以不用年级字段；
- 不支持确定性判题的内容明确标注评测能力边界；
- 历史内容版本不会被更新覆盖。

### M3：资料导入与知识地图生成

目标：让用户围绕自己的教材或资料学习。

交付：

- PDF、Markdown 和目录文本导入；
- 文档章节与知识点候选提取；
- 来源引用和页码定位；
- 用户确认、合并、拆分和修正知识点；
- AI 生成练习候选与答案校验工作流；
- 导入失败、扫描件和超大文件的降级处理。

退出标准：

- 用户可从一份大学教材目录生成可编辑知识地图；
- AI 总结与原文来源清晰区分；
- 未验证练习不能进入高置信度掌握判断；
- 删除资料时能处理其派生内容和历史证据关系。

### M4：“我的设置”能力闭环

目标：让用户真正控制自己的内容和 AI 私教。

交付：

- 学习偏好持久化；
- 内容包选择、切换、停用和学习目标；
- AI 私教讲解风格、追问方式、深度和语音偏好；
- 可用时间与计划强度；
- 数据导出、保留期和彻底删除；
- 设置变化的版本与审计。

退出标准：

- 设置跨设备恢复；
- 教师风格改变讲解编排但不改变判题结果；
- 切换内容包不会污染其他内容的知识地图；
- 用户能导出和删除自己的学习数据。

### M5：多学科学习运行时

目标：让同一私教框架支持不同类型的学习任务。

状态：核心运行时与五类学科能力基线完成，生产级能力加固待继续推进。

本轮已完成：

- 摸底、知识模型、计划、五阶段会话、作答证据和间隔复习改为从活动内容包读取；
- 新增计算机逻辑内容包的确定性选择题插件，并显式声明不支持步骤、语音和视觉交互；
- 数学与计算机逻辑分别保存知识状态、学习模型、计划、会话和复习主题，切换内容包时互不污染；
- 学科插件缺失、抛错或不支持确定性判题时拒绝写入作答与掌握证据；
- 新增 `evidenceEligible`、`evidenceTier` 和结构化 `evaluation` 合同，允许给出练习反馈而不污染掌握度；
- 数学插件支持作者审核检查点的逐步骤判题，并返回首个错误步骤；
- 语言插件支持作者量表语义匹配，低置信度语音只给反馈、不进入掌握证据；
- 编程插件使用自建受限算术表达式解释器运行测试，不调用 `eval`、`vm` 或系统进程；
- 概念型课程插件支持来源标记、分维度开放题量表和缺引用降级；
- HTTP 端到端测试覆盖数学证据保留、非数学摸底到复习闭环、五类插件反馈、证据门禁、双向切换恢复和插件失败保护。

后续加固：

- 数学符号等价推导与更丰富的步骤错误分类；
- 语言听力素材、口语发音维度和外部语义模型校准；
- 将编程沙箱扩展到通用语言运行时，并增加进程、网络、文件和资源隔离；
- 接入课程检索、真实案例库、人工复核队列和评分一致性评测。

交付：

- 数学：公式、步骤判题和数学视觉；
- 语言：听说读写和语义评测；
- 编程：代码运行、测试和错误解释；
- 概念型专业课：检索、口头解释、案例和开放题评分量表；
- 学科能力声明与安全降级。

退出标准：

- 至少一个数学内容包和一个非数学内容包完成端到端验证；
- 不同学科复用计划、会话、证据和复习主合同；
- 学科插件失败不会产生虚假掌握证据。

### M6：版本化判题与评测回归

目标：让每一次判题都可解释、可复现、可回放，并用独立评测集阻止错误结果污染掌握度。

状态：数学与语言切片完成，编程与概念型课程评测加固待继续推进。

本切片已实现：

- 统一评测合同，记录 schema、评测器版本、内容版本、量表版本、置信度、复核状态和判定指纹；
- 插件返回不完整或无置信度时关闭证据写入，避免以未知质量结果更新掌握度；
- 数学线性方程步骤判题升级为符号等价判断，接受不同但等价的解题路径和合法跨步；
- 首个错误步骤区分单边变形、符号、算术、非等价变形、非线性和不完整求解；
- 建立版本化数学黄金集与离线回放命令 `pnpm eval:private-tutor`，统计误判、漏判和证据泄漏；
- HTTP 返回完整评测来源，学习界面优先显示具体错误步骤。
- 语言语义判题升级为作者约束的 `causal-semantic-v2`，支持同义改写、否定检测和因果方向检查；
- 语义置信度与语音识别置信度联合校准，内容正确但语音置信度不足时保留反馈并禁止写入掌握度；
- 建立版本化语言黄金集，覆盖正确改写、缺失要素、否定、反向因果、冲突连接词和高低置信度语音。

后续切片：

- 扩充语言真实作答标注集，并把 `reviewStatus=required` 接入人工复核队列；
- 将编程判题接入隔离的通用语言运行时，补齐网络、文件、进程和资源配额边界；
- 为概念型课程开放题建立锚点样例、双评一致性和量表版本迁移；
- 增加跨版本回放报告，评测器升级未达门禁时禁止发布。

退出标准：

- 四类学科都有版本化黄金集和可重复执行的回放入口；
- 确定性判题误判率、漏判率和证据泄漏率有显式发布阈值；
- 低置信度与不支持输入只提供练习反馈，不更新掌握度；
- 历史作答可按原内容版本、量表版本和评测器版本解释判定结果。

## 6. 立即停止的工作

方向迁移期间不再继续投入：

- 多孩子切换体验；
- 监护邀请与家长权限扩展；
- 家长周报、家庭通知和家长压力指标；
- 儿童模式 PIN 与退出体验；
- 面向家庭试点的知情同意和儿童队列功能；
- 把教研、运营、安全或系统管理员作为普通用户可见的私教角色。

已完成代码暂不立即删除。先从产品路由和新数据写入中停用，待 M1 迁移测试覆盖后再清理，避免误删现有工作区数据或破坏学习核心。

## 7. 保留的质量门禁

- 内容来源和版本可追溯；
- 已知严重事实错误为零；
- 跨账号数据泄漏为零；
- 低置信度语音不直接判错；
- 提示后答对不直接标记掌握；
- AI 生成内容不冒充权威教材内容；
- 数据导出与删除可验证；
- 学习会话可暂停、恢复和幂等重试；
- 无障碍和弱网降级不丢失已确认学习证据。

若未来另行推出未成年人版本，再单独增加监护同意、儿童安全和家庭功能，不让其重新侵入通用产品主模型。

## 8. 测试迁移

新增或调整：

1. 登录用户首次创建唯一学习档案。
2. 已有档案的用户直接进入学习。
3. 用户看不到多孩子、监护、家庭或 PIN 入口。
4. 账号 A 不能读取账号 B 的学习档案。
5. 旧 learner 数据可迁移且证据数量一致。
6. 设置中的内容和 AI 教师偏好可恢复。
7. 数学内容包完成现有摸底到延迟复测闭环。
8. 大学课程内容包不依赖年级字段。
9. 用户资料导入保留来源并允许修正知识图谱。
10. AI 风格变化不影响确定性判题和掌握证据。

逐步淘汰：

- 多孩子隔离 UI 测试；
- 儿童模式路由与 PIN 旅程；
- 家庭周报与监护邀请组件测试；
- 家庭试点队列和儿童回访界面测试。

跨账号授权、数据删除和审计测试继续保留，但改用个人学习档案语义。

## 9. 下一开发切片

1. 定义编程沙箱适配器与威胁模型，保持现有安全解释器作为降级路径。
2. 为概念型开放题增加评分锚点、复核状态和评分一致性报告。
3. 将各学科黄金集接入发布检查，阻止评测退化版本进入主分支。
4. 扩充语言真实作答标注集，并实现人工复核结果回写与重新计证据。
5. 在黄金集扩充后，再把数学步骤合同扩展到方程组、不等式与含参数方程。

## 10. 提交与迁移条件

本方向切片可以提交前必须满足：

- 新的产品需求和开发计划一致；
- 私教入口不再要求家长或儿童模式；
- 现有学习闭环测试通过；
- 新增单用户入口与设置测试；
- 类型检查、lint、生产构建和差异检查通过；
- 没有删除或覆盖现有用户学习证据；
- 旧家庭 API 明确标记为兼容代码，不再被新 UI 调用。

彻底删除 guardian、child-mode 和家庭试点数据结构属于 M1 之后的独立迁移，不与本次方向调整混在同一提交中。
