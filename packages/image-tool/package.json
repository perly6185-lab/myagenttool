{
  "name": "@myagenttool/image-tool",
  "version": "0.0.0",
  "private": true,
  "description": "Shared image edit/generate capability: one core, used by codex (CLI) and claude (MCP).",
  "type": "module",
  "bin": {
    "myagent-image": "./cli.mjs"
  },
  "scripts": {
    "dev": "node ./cli.mjs --help",
    "test": "node ./cli.mjs --help"
  }
}
