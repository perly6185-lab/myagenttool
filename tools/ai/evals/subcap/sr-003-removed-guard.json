{
  "id": "sr-003-removed-guard",
  "kind": "review",
  "pr": {
    "title": "refactor(sessions): simplify session resume path",
    "body": "Removes redundant checks in resumeSession; the caller already validated input shape.",
    "diff": "diff --git a/apps/server/src/services/sessions.mjs b/apps/server/src/services/sessions.mjs\n--- a/apps/server/src/services/sessions.mjs\n+++ b/apps/server/src/services/sessions.mjs\n@@ -28,10 +28,6 @@ export function resumeSession({ state, sessionId, actor }) {\n   const session = state.sessions.find((entry) => entry.id === sessionId);\n-  if (!session) {\n-    return { error: \"session_not_found\" };\n-  }\n-  if (session.userId !== actor.userId) {\n-    return { error: \"forbidden\" };\n-  }\n   session.resumedAt = new Date().toISOString();\n   return { session };\n }"
  },
  "oracle": {
    "mustFlagFiles": [
      "sessions.mjs"
    ],
    "mustMention": [
      [
        "undefined",
        "null",
        "not found",
        "crash",
        "throw",
        "typeerror"
      ],
      [
        "forbidden",
        "authoriz",
        "another user",
        "other user",
        "ownership",
        "owner",
        "actor"
      ]
    ],
    "requireApproveFalse": true
  }
}
