{
  "name": "@myagenttool/tools-github",
  "version": "0.0.0",
  "private": true,
  "description": "GitHub governance, issue hygiene, PR, and Project field tooling.",
  "type": "module",
  "scripts": {
    "dev": "node ./src/index.mjs",
    "test": "node ./src/index.mjs --check && node --test test/*.test.mjs",
    "lint": "node ./src/index.mjs --check",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test:unit": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
