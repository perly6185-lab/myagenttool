# Local issue closeout visual QA

Captured from the real Vite console after `networkidle` plus a 1.2 second data-render
settle. Both images show the loaded Approvals queue backed by the local server.

- `desktop.png` — 1440 × 1000
- `mobile.png` — 390 × 844

The mobile capture uses `?section=approvals` so it validates the responsive decision
queue itself rather than an unrelated default screen.
